<% @messages.each do |message| %>
	<div class="col-md-10 col-md-offset-1">
		<div class="message">
			<h2><%= message.title %></h2>
			<%= link_to "Jon Convo", message, class: "btn btn-primary" %>
		</div>
	</div>
<% end %>